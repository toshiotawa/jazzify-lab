/** サバイバル本番セリフ吹き出し（Web）。OnboardingOverlays の text-sm と同等。 */
export const SURVIVAL_SPEECH_BUBBLE_FONT_PX = 14;
export const SURVIVAL_SPEECH_BUBBLE_LINE_HEIGHT_PX = 20;

/** ファイ用最大幅。ジャ爺は 2/3。 */
export const SURVIVAL_FAI_BUBBLE_MAX_WIDTH_PX = 380;
export const SURVIVAL_JAJII_BUBBLE_MAX_WIDTH_PX = Math.round(
  SURVIVAL_FAI_BUBBLE_MAX_WIDTH_PX * (2 / 3),
);
