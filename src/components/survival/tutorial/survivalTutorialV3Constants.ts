/** v3 チュートリアル共通タイミング（iOS `SurvivalTutorialV3Constants` と揃える）。 */

/** 出題前セリフの自動送り秒数。タップで即スキップ可。 */
export const SURVIVAL_TUTORIAL_V3_INTRO_HOLD_SECONDS = 3;

/** セリフのみの 1 行あたりの自動送り秒数。 */
export const SURVIVAL_TUTORIAL_V3_DIALOGUE_LINE_SECONDS = 3;

/** 正解後の余韻（iOS `SurvivalTutorialV3Constants.afterCorrectSeconds` と揃える）。 */
export const SURVIVAL_TUTORIAL_V3_AFTER_CORRECT_SECONDS = 1;

/** フレーズバトル reveal 時の静止敵数（譜面背後の視認性確保）。 */
export const SURVIVAL_TUTORIAL_V3_PHRASE_REVEAL_ENEMY_COUNT = 1;

/** フレーズバトル reveal 時の静止敵リング半径（px）。 */
export const SURVIVAL_TUTORIAL_V3_PHRASE_REVEAL_ENEMY_RADIUS = 180;

/** play(一緒に弾かせる): 休符塊(会話のみ小節)の自動送り秒数。タップで即送り可。 */
export const SURVIVAL_TUTORIAL_V3_PLAY_REST_SECONDS = 3;

/** play: 最後のノーツ正解後にスキップボタンを出して待つ秒数（押さなければ自動）。 */
export const SURVIVAL_TUTORIAL_V3_PLAY_END_SKIP_SECONDS = 2;
