import CoreGraphics

/// サバイバル本番セリフ吹き出し幅。ジャ爺はファイ（耳コピチュートリアル既定）の 2/3。
enum SurvivalSpeechBubbleLayout {
    static let faiMaxBubbleWidth: CGFloat = EarTrainingTutorialDialogLayoutConstants.maxBubbleWidth
    static let jajiiMaxBubbleWidth: CGFloat = faiMaxBubbleWidth * 2 / 3
}
